apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-sonarqube-postgresql-0
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
